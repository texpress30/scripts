fastapi==0.115.0
python-multipart==0.0.12
uvicorn[standard]==0.30.6
# Railway production: use psycopg binary wheel to avoid build-time libpq compilation issues
psycopg[binary]==3.2.1
psycopg_pool>=3.2.0

google-ads==25.1.0

cryptography==43.0.1
boto3==1.35.36
pymongo==4.8.0
Pillow>=11.0.0
httpx>=0.27.0
anthropic>=0.40.0

# Async job queue for background removal + preview rendering.
# Redis is already in the stack as broker/result backend.
celery[redis]==5.4.0
redis>=5.0.0

# Background removal pipeline (U^2-Net ONNX via rembg).
# onnxruntime is pinned to the CPU-only wheel; swap to onnxruntime-gpu when a GPU worker is provisioned.
rembg==2.0.59
onnxruntime==1.19.2
numpy>=1.26.0
