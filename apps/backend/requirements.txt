fastapi==0.115.0
python-multipart==0.0.12
uvicorn[standard]==0.30.6
# Railway production: use psycopg binary wheel to avoid build-time libpq compilation issues
psycopg[binary]==3.2.1
psycopg_pool>=3.2.0

google-ads==25.1.0

cryptography==43.0.1
boto3==1.35.36
pymongo==4.8.0
Pillow>=11.0.0
httpx>=0.27.0
anthropic>=0.40.0

# Async job queue for background removal + preview rendering. Lightweight —
# all services that dispatch Celery tasks need celery[redis] on the PYTHONPATH
# so the kombu serializer can pickle task payloads. Redis is already the
# broker for the rest of the stack.
celery[redis]==5.4.0
redis>=5.0.0

# NOTE: rembg + onnxruntime + numpy live in requirements-bgworker.txt instead
# of this base file. They pull in ~500MB of transitive dependencies (opencv,
# scipy, scikit-image, numba, llvmlite, sympy, pymatting) and are only needed
# inside the ``worker-bgremoval`` container. Keeping them out of the base
# image saves roughly 800MB on every other service (api-backend, render
# workers, sync workers, cron jobs) and makes their cold-starts noticeably
# faster on Railway.
