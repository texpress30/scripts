# Server-side target used by Next.js API proxy (/api/* -> backend)
BACKEND_API_URL=http://localhost:8000

# Optional: keep only for direct browser calls (not recommended in production)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
